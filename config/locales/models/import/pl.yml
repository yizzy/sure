---
pl:
  activerecord:
    attributes:
      import:
        currency: Waluta
        number_format: Format liczby
    errors:
      models:
        import:
          attributes:
            raw_file_str:
              invalid_csv_format: nie jest prawidłowym formatem CSV
